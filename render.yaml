services:
  - type: web
    name: mrafrix-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PORT
        value: 8080
    autoDeploy: true